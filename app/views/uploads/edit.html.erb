<% content_for :title do %>
  <%= t('upload.edit.title', :title => "") %>
  <small><%= @upload %></small>
<% end %>

<%= render 'form' %>

<% content_for :sidebar_menu do %>
  <%= back_link([@resource, @upload]) %>
<% end %>